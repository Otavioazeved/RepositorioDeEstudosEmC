#include <stdio.h>
#include <stdlib.h>

int main() {
    int a, b, soma;
printf("Digite A:");
scanf("%d", &a);
printf("Digite B");
scanf("%d", &b);
soma = a + b;
printf("A soma de A + B: %d\n", soma);

    return 0;
}

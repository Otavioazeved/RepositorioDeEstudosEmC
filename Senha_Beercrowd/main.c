#include <stdio.h>
// Programa que imprime um numero perto de uma senha digitada:
//trabalho no Beercrowd da minha faculdade

int main() {
    int n;
    while (scanf("%d", &n) != EOF) {
        printf("%d\n", n - 1);
    }
    return 0;
}
]

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b, s;
    printf("digite dois numeros inteiros: ");
    scanf("%d%d", &a, &b, &s);
    s = a + b;
    printf("a soma entre os dois inteiros e: %d ", s);


    return 0;
}
